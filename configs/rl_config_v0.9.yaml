data:
  # 留空，请在训练脚本 train_rl_v0.9 中设置 DATASET_DIR
  dataset_dir: ""
  # dataset_dir 下应包含: train_dataset.npz, test_dataset.npz, metadata.json
  # 数据粒度说明: 由 prepare_rl_data_v0.3 的 --resample 参数决定。
  #   若传 --resample W-FRI，则每行=1周；若不传，则每行=1交易日。

trading:
  initial_cash: 10000000.0
  transaction_cost: 0.001
  max_weight_per_stock: 0.10
  softmax_temperature: 1.0

rl:
  algorithm: "PPO"
  total_timesteps: 50000
  learning_rate: 0.0003
  batch_size: 32
  n_steps: 512
  n_epochs: 10
  gamma: 1.0   # 不折扣远期收益，追求长期回报
  gae_lambda: 0.95
  clip_range: 0.2
  ent_coef: 0.01
  vf_coef: 0.5
  max_grad_norm: 0.5
  hidden_dim: 64
  n_heads: 2
  temporal_window: 50   # v0.9 默认 30 (周频=30周, 日频=30交易日)
  normalize_observations: true
  normalize_rewards: false
  device: "cuda"

reward:
  type: "log_return"
  turnover_penalty: 0.5
